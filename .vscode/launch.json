{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch with MSVC debugger",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "console": "externalTerminal"
    }
  ]
}